# serveflow-db

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build serveflow-db` to build the library.

## Running unit tests

Run `nx test serveflow-db` to execute the unit tests via [Jest](https://jestjs.io).
