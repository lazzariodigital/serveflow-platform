import { createClasses } from '../../theme/create-classes';

// ----------------------------------------------------------------------

export const iconifyClasses = {
  root: createClasses('icon__root'),
  flag: createClasses('icon__flag'),
};
